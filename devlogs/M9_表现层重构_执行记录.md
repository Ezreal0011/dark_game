# M9 表现层重构执行记录

日期：2026-05-03

## M9-0：安全检查与执行边界确认

### 当前基线

- Git 分支：`main`
- Git 状态：开始 M9-0 时本地与 `origin/main` 同步
- 基线提交：`264ca4f feat: complete M8 final validation`
- Godot 版本：4.6.2 stable
- 主场景：`res://scenes/Main.tscn`
- 渲染路径：`gl_compatibility`

### 当前项目结构摘要

- `assets/`：现有基础资源目录，后续 M9 可扩展背景、装饰、点位、单位、UI、FX 子目录。
- `configs/`：玩法配置表，包括 `game_config.json`、`skill_config.json`、`tech_config.json`。
- `scenes/`：主场景和瓦片集。
- `scripts/`：M0-M8 核心玩法脚本。
- `tests/`：M4、M5、M6、M8 自动测试。
- `ui/`：当前旧 HUD 场景。
- `devlogs/`、`docs/`：开发日志和验收文档。

### 核心玩法脚本清单

本轮表现层重构不直接重写以下文件：

1. `res://scripts/turn_manager.gd`
2. `res://scripts/signal_manager.gd`
3. `res://scripts/skill_manager.gd`
4. `res://scripts/npc_ai_controller.gd`
5. `res://scripts/grid_map.gd`
6. `res://scripts/game_controller.gd`

如后续阶段确实需要改动，只允许做极小的表现层挂接，例如发出刷新通知、调用表现层接口、读取当前状态；不允许改变移动、扫描、攻击、技能、AI、黑域、胜负等玩法判定。

### M9 推荐新增目录

后续阶段可新增：

- `res://presentation/controllers/`
- `res://presentation/layers/`
- `res://presentation/resources/`
- `res://presentation/ui/`
- `res://presentation/ui/widgets/`
- `res://assets/backgrounds/`
- `res://assets/decorations/`
- `res://assets/points/`
- `res://assets/units/`
- `res://assets/ui/`
- `res://assets/ui/icons/`
- `res://assets/fx/`
- `res://assets/minimap/`

### 运行检查结果

- 主场景 headless 启动：通过。
- M8 最终自检：通过。
- 已知提示：Godot headless 退出时出现 `ObjectDB instances leaked at exit` warning；当前未发现脚本解析错误、节点路径错误或主场景启动失败。

### M9-0 结论

项目当前可作为表现层重构基线。下一阶段应进入 M9-1：表现层工程骨架搭建。必须等待人工确认后再继续。

## M9-1：表现层工程骨架搭建

### 本阶段目标

新增表现层目录、控制器、视觉层脚本、资源 manifest 和新 HUD 根节点，让后续 M9-2 到 M9-7 的视觉改造有清晰落点。本阶段只做空模块接入，不替换旧 HUD，不生成正式美术资源，不改变玩法逻辑。

### 新增目录

- `res://presentation/controllers/`
- `res://presentation/layers/`
- `res://presentation/resources/`
- `res://presentation/ui/`
- `res://presentation/ui/widgets/`
- `res://assets/backgrounds/`
- `res://assets/decorations/`
- `res://assets/points/`
- `res://assets/units/`
- `res://assets/ui/`
- `res://assets/ui/icons/`
- `res://assets/fx/`
- `res://assets/minimap/`

### 新增脚本

- `res://presentation/controllers/battle_view_controller.gd`
- `res://presentation/controllers/hud_controller.gd`
- `res://presentation/controllers/fx_controller.gd`
- `res://presentation/controllers/minimap_controller.gd`
- `res://presentation/layers/presentation_layer.gd`
- `res://presentation/layers/background_layer.gd`
- `res://presentation/layers/grid_visual_layer.gd`
- `res://presentation/layers/zone_visual_layer.gd`
- `res://presentation/layers/point_visual_layer.gd`
- `res://presentation/layers/unit_visual_layer.gd`
- `res://presentation/layers/action_preview_layer.gd`
- `res://presentation/layers/fx_layer.gd`

### 新增资源配置

- `res://presentation/resources/art_manifest.json`
- `res://presentation/resources/icon_manifest.json`
- `res://presentation/resources/ui_theme.json`
- `res://presentation/resources/zone_style.json`

### 新增 UI 根节点

- `res://presentation/ui/HUDRoot.tscn`

当前 `HUDRoot` 默认隐藏，避免影响旧 HUD。后续 M9-3 再逐步接管 UI。

### 修改文件

- `res://scenes/Main.tscn`

修改内容：新增 `Presentation` 节点，并挂接 BackgroundLayer、GridVisualLayer、ZoneVisualLayer、PointVisualLayer、UnitVisualLayer、ActionPreviewLayer、FxLayer、HUDController、FxController、MiniMapController；新增隐藏的 `HUDRoot` 实例。旧 `HUD` 仍保留并继续负责当前玩法 UI。

### 运行检查结果

- 主场景 headless 启动：通过。
- M8 最终自检：通过。
- 首次检查发现新脚本强类型 class_name 缓存顺序问题，已改为 M9-1 阶段更稳妥的弱类型 Node 接口。
- 当前未发现脚本解析错误、节点路径错误或主场景启动失败。

### M9-1 结论

表现层骨架已接入且不影响 M0-M8 玩法。下一阶段应进入 M9-2：战场视觉基底重构。必须等待人工确认后再继续。

## M9-2：战场视觉基底重构

### 本阶段目标

让主战场截图明显脱离原型感，先完成深空背景、星云叠加、陨石/残骸装饰、发光网格、安全区/黑域/静默区/回声区视觉分层。本阶段不重构 HUD，不替换点位和单位资源，不改变玩法逻辑。

### 新增背景资源

- `res://assets/backgrounds/bg_space_main.png`
- `res://assets/backgrounds/bg_nebula_01.png`
- `res://assets/backgrounds/bg_nebula_02.png`
- `res://assets/backgrounds/bg_nebula_03.png`

### 新增装饰资源

- `res://assets/decorations/deco_asteroid_01.png` 到 `res://assets/decorations/deco_asteroid_12.png`
- `res://assets/decorations/deco_wreck_01.png` 到 `res://assets/decorations/deco_wreck_08.png`

### 修改表现层

- `res://presentation/layers/background_layer.gd`
  - 读取 `art_manifest.json`。
  - 绘制深空背景、星云层、陨石和残骸装饰。
- `res://presentation/layers/grid_visual_layer.gd`
  - 按 `GridMap` 的地图尺寸和 `tile_size` 绘制青蓝发光网格。
  - 网格与瓦片中心对齐。
- `res://presentation/layers/zone_visual_layer.gd`
  - 读取 `zone_style.json`。
  - 绘制安全区边界、静默区、回声区和黑域覆盖。
- `res://presentation/resources/art_manifest.json`
  - 登记背景和装饰资源路径。

### 修改旧资源

- `res://assets/m1_grid_atlas.svg`

修改原因：旧瓦片底色不透明，会盖住新深空背景。本阶段将瓦片底色改为半透明，保留线框和障碍可读性，让太空背景进入主画面。

### 修改核心脚本挂接

- `res://scripts/game_controller.gd`

仅新增表现层绑定和刷新调用：

- `_bind_presentation()`
- `_refresh_presentation()`

该改动只让表现层读取当前地图状态并刷新视觉，不改变移动、扫描、攻击、技能、AI、黑域、胜负等玩法判定。

### 截图效果说明

运行主场景后，主战场底部应出现深空背景和星云层，地图上叠加青蓝科技网格；静默区显示蓝色半透明覆盖，回声区显示橙色半透明覆盖，黑域随缩圈后显示红色危险覆盖；陨石和残骸作为背景装饰分布在战场周围。

### 运行检查结果

- 主场景 headless 启动：通过。
- M8 最终自检：通过。
- M6 AI 测试：通过。
- 当前未发现脚本解析错误、节点路径错误或主场景启动失败。
- 已知提示：Godot headless 退出时仍出现 `ObjectDB instances leaked at exit` warning，和之前一致。

### M9-2 结论

战场视觉基底已接入，项目从纯原型格子画面开始转向太空战术界面。下一阶段应进入 M9-3：HUD 框架重构。必须等待人工确认后再继续。

### M9-2 返工修复：参考图风格对齐

人工验收反馈：第一版深空背景、星云和装饰物过于粗糙，风格不贴近参考图。

本次修复内容：

1. 重新生成 `bg_space_main.png`：
   - 改为更暗的深蓝黑星域。
   - 增加细碎恒星和轻微银河尘带。
   - 在左右边缘加入低透明红色侵蚀气氛。
   - 保留左下角弱化星球弧光，贴近参考图构图。
2. 重新生成 `bg_nebula_01.png` 到 `bg_nebula_03.png`：
   - 星云改为更薄、更窄、更暗的氛围层。
   - 不再使用大块彩色雾团，避免抢地图信息。
3. 重新生成 `deco_asteroid_01.png` 到 `deco_asteroid_12.png`：
   - 陨石改为更小比例、更暗、更接近太空碎片的材质。
4. 重新生成 `deco_wreck_01.png` 到 `deco_wreck_08.png`：
   - 残骸改为狭长金属碎片风格。
5. 调整 `background_layer.gd`：
   - 降低星云、陨石、残骸透明度。
   - 缩小装饰物尺寸，让其成为背景气氛而不是主视觉。
6. 调整 `grid_visual_layer.gd` 和 `zone_style.json`：
   - 压低网格与区域填充亮度，让整体更接近参考图的克制科技 HUD 风格。

修复后运行检查：

- 主场景 headless 启动：通过。
- M8 最终自检：通过。

### M9-3 返工修复：地图整体进入 HUD 安全区

人工验收反馈：即使压缩 HUD，地图有效格子仍会延伸到 UI 下方。决定采用更稳定的方案：让战场本身缩放并平移到 HUD 安全区内。

本次修复内容：

1. 修改 `res://scenes/Main.tscn`：
   - `GridMap.position` 调整为 `Vector2(170, 150)`。
   - `GridMap.scale` 调整为 `Vector2(0.88, 0.88)`。
2. 修改 `res://presentation/layers/grid_visual_layer.gd`：
   - 网格绘制按 `GridMap.scale` 计算。
   - 保证发光网格继续和缩放后的瓦片对齐。
3. 修改 `res://presentation/layers/zone_visual_layer.gd`：
   - 区域覆盖按 `GridMap.scale` 计算。
   - 保证静默区、回声区、黑域、安全区继续和瓦片对齐。

修复后运行检查：

- 主场景 headless 启动：通过。
- M4 规则测试：通过。
- M8 最终自检：通过。

### M9-3 返工修复：底部遮挡与表现层错位

人工验收反馈：底部行动栏仍遮挡地图，且地图/表现层有错位感。

本次修复内容：

1. 再次调整 `res://scenes/Main.tscn`：
   - `GridMap.position` 调整为 `Vector2(164, 122)`。
   - `GridMap.scale` 调整为 `Vector2(0.68, 0.68)`。
   - 目标是让地图底边明确停在底部 HUD 上方。
2. 修改 `res://presentation/layers/grid_visual_layer.gd`：
   - 不再手算 `grid_map.position + local * scale`。
   - 改用 `grid_map.grid_to_world()` 计算格子世界坐标。
   - 让发光网格跟随 TileMap 真实变换。
3. 修改 `res://presentation/layers/zone_visual_layer.gd`：
   - 区域覆盖同样改用 `grid_map.grid_to_world()` 对齐。
   - 静默区、回声区、黑域、安全区边界跟随 TileMap 真实变换。

修复后运行检查：

- 主场景 headless 启动：通过。
- M4 规则测试：通过。
- M8 最终自检：通过。

## M9-3：HUD 框架重构

### 本阶段目标

搭建参考图方向的战术 HUD 框架：顶部状态栏、左侧信息面板、底部行动栏、右侧科技树、右下小地图。为保证玩法不被破坏，本阶段采用旧 HUD 兼容方式：保留 `res://ui/HUD.tscn` 和 `HUD` 信号接口，在 `hud.gd` 中运行时重排布局并统一样式。

### 新增资源

- `res://assets/ui/icons/icon_action_move.png`
- `res://assets/ui/icons/icon_action_scan.png`
- `res://assets/ui/icons/icon_action_stealth.png`
- `res://assets/ui/icons/icon_action_interfere.png`
- `res://assets/ui/icons/icon_action_attack.png`
- `res://assets/ui/icons/icon_action_overclock.png`
- `res://assets/ui/icons/icon_action_bag.png`
- `res://assets/ui/icons/icon_action_tech.png`
- `res://assets/ui/icons/icon_tech_stealth.png`
- `res://assets/ui/icons/icon_tech_recon.png`
- `res://assets/ui/icons/icon_tech_shock.png`

### 修改文件

- `res://presentation/resources/icon_manifest.json`
  - 登记行动图标和科技图标路径。
- `res://scripts/hud.gd`
  - 新增 M9 HUD 运行时布局。
  - 新增左侧信息面板。
  - 将行动按钮重排到底部行动栏。
  - 将科技面板移至右侧。
  - 调整小地图和日志面板位置。
  - 为按钮和面板增加深色半透明背景、青蓝描边、hover/pressed/disabled 状态。
  - 为行动按钮和科技按钮接入图标。

### 旧 HUD 兼容方式

本阶段不删除旧 `HUD.tscn`，也不改变 `wait_pressed`、`scan_pressed`、`attack_pressed`、`collect_pressed`、`pick_skill_pressed`、`tech_upgrade_pressed`、`skill_slot_pressed`、`end_turn_pressed`、`gm_toggled` 等信号。所有原有按钮仍使用原有信号链路触发玩法。

### 运行检查结果

- 主场景 headless 启动：通过。
- M4 规则测试：通过。
- M5 规则测试：通过。
- M6 AI 测试：通过。
- M8 最终自检：通过。
- 当前未发现脚本解析错误、节点路径错误或主场景启动失败。

### M9-3 结论

HUD 已形成五区结构：顶部状态、左侧信息、底部行动、右侧科技、小地图。下一阶段应进入 M9-4：点位与单位表现替换。必须等待人工确认后再继续。

### M9-3 返工修复：HUD 遮挡地图

人工验收反馈：HUD 面板遮挡地图有效区域。

本次修复内容：

1. 隐藏旧日志大面板，避免右侧中部大面积遮挡地图。
2. 缩小左侧信息面板宽高。
3. 缩小右侧科技面板宽高。
4. 缩小右下小地图面板。
5. 收窄并降低底部行动栏，减少遮挡底部格子。
6. 同步缩小 HUD 鼠标拦截范围，隐藏日志不再拦截地图点击。

修复后运行检查：

- 主场景 headless 启动：通过。
- M5 规则测试：通过。
- M8 最终自检：通过。

### M9-3 返工修复：HUD 继续遮挡地图

人工验收截图显示：左侧信息面板、右侧科技面板、右下小地图和底部行动栏仍压在地图有效格子上。

本次修复内容：

1. 左侧信息面板压缩到屏幕左侧约 150 像素内。
2. 右侧科技面板压缩到屏幕最右约 160 像素内。
3. 右下小地图压缩到屏幕最右约 160 像素内。
4. 底部行动栏下压到屏幕底部约 64 像素区域。
5. 行动按钮改为更短的单行文案。
6. 同步缩小 HUD 鼠标拦截范围，避免透明区域挡住地图点击。

修复后运行检查：

- 主场景 headless 启动：通过。
- M8 最终自检：通过。

### M9-2 返工修复：区域瓦片图案清理

人工验收反馈：静默区、回声区、黑域格子中仍有旧瓦片图案，和参考图的半透明区域表现不一致。

本次修复内容：

1. 修改 `res://assets/m1_grid_atlas.svg`。
2. 移除静默区瓦片的内部线条图案。
3. 移除回声区瓦片的圆形/核心图案。
4. 移除黑域瓦片的裂纹图案。
5. 将三类区域瓦片底色压到极低透明度。
6. 区域颜色和边框统一交给 `ZoneVisualLayer` 绘制，形成类似参考图的半透明区块表现。

修复后运行检查：

- 主场景 headless 启动：通过。
- M8 最终自检：通过。

### M9-2 返工修复：使用人工指定背景图

人工验收反馈：改用指定图片 `7c2b56cb-a39f-4a25-b5d8-4c1d62a1e038.png` 作为战场背景。

本次修复内容：

1. 将用户提供图片复制并覆盖为：
   - `res://assets/backgrounds/bg_space_main.png`
2. 调整 `background_layer.gd`：
   - 只绘制 `bg_space_main.png`。
   - 关闭额外星云叠加。
   - 关闭额外陨石/残骸装饰叠加。
3. 保留 `art_manifest.json` 中的资源登记，后续如果需要重新启用分层装饰，可以继续使用 manifest 管理。

修复后运行检查：

- 主场景 headless 启动：通过。
- M8 最终自检：通过。
### M9-3 返工修复：地图放大并重新居中

人工验收反馈：上一版地图虽然避开了 HUD，但整体偏左且过小，战场阅读感不足。
本次修复内容：
1. 调整 `res://scenes/Main.tscn`：
   - `GridMap.position` 调整为 `Vector2(279, 116)`。
   - `GridMap.scale` 调整为 `Vector2(0.78, 0.78)`。
   - 目标是在左侧信息面板、右侧科技/小地图面板、底部行动栏之间，让地图主体重新居中并增大。
2. 保持 `GridVisualLayer` 和 `ZoneVisualLayer` 继续使用 `grid_map.grid_to_world()` 对齐 TileMap。
3. 本次不进入 M9-4，只修复 M9-3 HUD 安全区布局问题。

修复后运行检查：

- 主场景 headless 启动：通过。
- M4 规则测试：通过。
- M8 最终自检：通过。
## M9-4：点位与单位表现替换

### 本阶段目标

将资源点、技能点、玩家单位、NPC 单位从旧原型几何图形替换为更接近参考图的战术 HUD 表现。只替换表现层，不改采集、技能点拾取、NPC AI、胜负、黑域等玩法逻辑。

### 新增文件

- 无。

### 修改文件

- `res://presentation/layers/point_visual_layer.gd`
  - 新增资源点表现：半透明范围底板、金色六边核心、资源点文字与暗能收益提示。
  - 新增技能点表现：半透明范围底板、紫色菱形核心、等级与技能点文字提示。
- `res://presentation/layers/unit_visual_layer.gd`
  - 新增玩家单位表现：蓝色发光战术标记、玩家名称、HP 状态条。
  - 新增 NPC 单位表现：敌对橙色战术标记、暴露/GM 显示状态、NPC 名称与 HP 状态条。
  - GM 开启时继续显示所有存活 NPC；GM 关闭时只显示玩家附近已暴露 NPC。
- `res://scripts/game_controller.gd`
  - 隐藏旧 `Player`、`NPCs`、`PointLayer` 原型图形。
  - 在玩家位置、NPC 状态、地图点位变化后刷新表现层。
  - 不修改任何核心玩法判定。

### 资源清单

- 本阶段没有新增位图资源。
- 点位和单位使用 Godot 原生 `Polygon2D`、`Line2D`、`Label`、`ColorRect` 程序化绘制，便于后续继续替换为正式美术资源。

### 运行检查结果

- 主场景 headless 启动：通过。
- M4 规则测试：通过。
- M5 规则测试：通过。
- M6 AI 测试：通过。
- M8 最终自检：通过。
- 当前未发现脚本解析错误、节点路径错误或玩法自检失败。

### 人工验收建议

1. 启动主场景，确认玩家不再是旧的纯蓝三角，而是蓝色战术标记并带名称/HP。
2. 确认资源点显示为金色战术点，并显示“资源点”和暗能收益。
3. 确认技能点显示为紫色战术点，并显示等级。
4. 关闭 GM 时，NPC 只在玩家附近出现；开启 GM 后，所有存活 NPC 都能看见，并显示名称和 HP。
5. 移动、采集、拾取技能点后，点位和单位表现会同步刷新。

### 已知问题

- 单位和点位仍是程序化战术图形，还不是最终商业级精修图标。
- 本阶段未处理攻击/扫描/技能释放特效，那属于 M9-5 战术反馈阶段。

### 下一阶段建议

人工确认 M9-4 后，进入 M9-5：战术反馈表现，重点处理攻击线、扫描波、技能范围、命中/未命中、采集/拾取等交互反馈。
## M9-5：战术反馈表现

### 本阶段目标

补齐核心交互反馈，让扫描、攻击、技能范围、技能释放、资源采集、技能点拾取、NPC 移动都具备清晰的屏幕反馈。所有反馈仍走表现层，不改玩法判定。

### 新增文件

- 无。

### 修改文件

- `res://presentation/layers/action_preview_layer.gd`
  - 新增范围格半透明高亮。
  - 新增目标框。
  - 新增从玩家到目标的战术指示线。
  - 新增范围/目标浮动文字。
- `res://presentation/layers/fx_layer.gd`
  - 新增扫描脉冲环。
  - 新增攻击光束、命中/未命中文字、目标爆闪。
  - 新增采集暗能反馈。
  - 新增技能点拾取反馈。
  - 新增技能释放爆闪。
  - 新增 NPC 移动轨迹反馈。
- `res://presentation/controllers/fx_controller.gd`
  - 新增预览和特效转发接口。
- `res://presentation/controllers/battle_view_controller.gd`
  - 新增 M9-5 战术反馈入口，供 `GameController` 调用。
- `res://scripts/game_controller.gd`
  - 在扫描、攻击、采集、拾取技能点、技能释放、NPC 移动时调用表现层反馈。
  - 隐藏旧扫描/技能预览层，避免旧原型图形与新表现层重复显示。
  - 不修改行动消耗、命中、采集、技能、NPC AI 等核心规则。

### 资源清单

- 本阶段没有新增位图资源。
- 战术反馈使用 `Line2D`、`Polygon2D`、`Label` 程序化绘制，便于后续按参考图继续精修。

### 运行检查结果

- 主场景 headless 启动：通过。
- M4 规则测试：通过。
- M5 规则测试：通过。
- M6 AI 测试：通过。
- M8 最终自检：通过。
- 当前未发现脚本解析错误、节点路径错误或玩法自检失败。

### 人工验收建议

1. 点击“扫描”，确认玩家中心出现扫描范围和紫色/青色脉冲反馈。
2. 点击“攻击”后悬停/选择目标，确认出现攻击范围、目标框和玩家到目标的指示线。
3. 攻击执行后，确认命中或未命中都有光束、爆闪和文字反馈。
4. 站在资源点点击“采集”，确认出现暗能获得反馈。
5. 站在技能点点击“技能”，确认出现技能点拾取反馈，并继续弹出三选一技能。
6. 使用技能时，确认技能范围和影响格有示意，释放时有爆闪反馈。
7. 结束回合后，确认 NPC 逐个行动时有移动轨迹反馈。

### 已知问题

- 反馈样式仍是程序化 HUD 图形，未进入最终精修动画。
- 本阶段没有增加音效、震屏或粒子系统；这些可以放在 M9-7 表现打磨阶段。

### 下一阶段建议

人工确认 M9-5 后，进入 M9-6：小地图与科技栏表现优化，重点补齐小地图真实绘制、右侧科技区状态、GM 观察信息和 HUD 信息密度。
## M9-6：小地图与科技栏表现优化

### 本阶段目标

补齐右侧 HUD 的战术信息密度：小地图从纯文字改为真实绘制，科技栏从普通按钮改为带等级和状态的科技卡片。只优化表现和信息呈现，不修改科技升级、GM、黑域、点位或 NPC 规则。

### 新增文件

- `res://presentation/ui/widgets/minimap_widget.gd`
  - 新增小地图绘制控件。
  - 绘制地图网格、安全区、黑域、玩家、NPC、资源点、技能点。

### 修改文件

- `res://presentation/controllers/minimap_controller.gd`
  - 读取 `GridMap` 和 `GameController` 当前状态。
  - 将地图尺寸、缩圈层数、玩家位置、NPC 可见位置、资源点、技能点传给 HUD。
- `res://scripts/hud.gd`
  - 在右下角小地图面板内挂载 `MiniMapWidget`。
  - 小地图文本改为图例和安全区信息。
  - 科技栏显示“侦察/移动/隐匿 Lv.x”和“可升级/锁定”状态。
  - 保留 GM 开关。
- `res://scripts/game_controller.gd`
  - 在技能 HUD 刷新时同步科技等级给 HUD。

### 资源清单

- 本阶段没有新增位图资源。
- 小地图使用 `Control._draw()` 程序化绘制，便于后续继续精修配色和图例。

### 运行检查结果

- 主场景 headless 启动：通过。
- M4 规则测试：通过。
- M5 规则测试：通过。
- M6 AI 测试：通过。
- M8 最终自检：通过。
- 当前未发现脚本解析错误、节点路径错误或玩法自检失败。

### 人工验收建议

1. 确认右下角小地图不再只是文字，而是能看到网格、安全区和黑域边界。
2. 确认小地图上蓝色为玩家，橙色为可见 NPC，黄色为资源点，紫色为技能点。
3. 开关 GM 后，确认小地图 NPC 显示随 GM 可见规则变化。
4. 采集资源点或拾取技能点后，确认小地图对应点位消失。
5. 科技升级后，确认右侧科技按钮等级变化，并显示是否可升级。

### 已知问题

- 小地图仍是简洁战术抽象图，不是最终参考图级别的美术面板。
- 科技栏目前只显示等级和可升级状态，尚未显示完整经验条/成本条；如需要可在 M9-7 打磨。

### 下一阶段建议

人工确认 M9-6 后，进入 M9-7：整体打磨，包括按钮状态、文字排版、反馈节奏、面板透明度、响应式避让和最后一轮视觉一致性修正。
### M9-6 返工修复：小地图比例校正

人工验收反馈：小地图显示比例不正确。
本次修复内容：
1. 调整 `res://scripts/hud.gd`：
   - 小地图面板从窄竖框改为更接近地图比例的横向面板。
   - `MiniMapWidget` 控件尺寸调整为 `180 x 120`，匹配默认地图 `24 x 16` 的 `3:2` 比例。
   - 同步扩大 HUD 鼠标拦截区域，避免点击小地图时误点地图。
2. 调整 `res://presentation/ui/widgets/minimap_widget.gd`：
   - 绘制时根据 `map_size.x / map_size.y` 自动锁定实际地图比例。
   - 即使控件尺寸变化，也保持小地图不拉伸、不压扁。

修复后运行检查：

- 主场景 headless 启动：通过。
- M8 最终自检：通过。
## M9-7：整体表现打磨

### 本阶段目标

在不修改玩法规则的前提下，做最后一轮界面体验打磨：按钮文案、状态提示、中心提示动效、文字阴影、文本溢出保护、科技卡提示和小地图比例返工后的交互安全区。

### 新增文件

- 无。

### 修改文件

- `res://scripts/hud.gd`
  - 中心提示增加淡入和轻微缩放动效。
  - 行动按钮统一为干净中文文案：待机、扫描、攻击、采集、技能、结束。
  - 行动按钮增加 tooltip，说明按钮用途。
  - 科技卡增加 tooltip，显示科技名、等级和当前状态。
  - 主要 HUD 文本增加阴影，提高深空背景上的可读性。
  - 按钮和标签增加文本溢出保护，降低窄屏或长文本时挤压风险。
  - 保留 M9-6 小地图比例修复后的 HUD 拦截区。

### 资源清单

- 本阶段没有新增资源。

### 运行检查结果

- 主场景 headless 启动：通过。
- M4 规则测试：通过。
- M5 规则测试：通过。
- M6 AI 测试：通过。
- M8 最终自检：通过。
- 当前未发现脚本解析错误、节点路径错误或玩法自检失败。

### 人工验收建议

1. 确认底部行动按钮显示为干净中文，没有乱码残留。
2. 鼠标悬停行动按钮和科技按钮时，确认 tooltip 可说明用途。
3. 确认“你的回合”“科技升级”“胜利/失败”等中心提示出现时有轻微动效。
4. 确认顶部状态栏、底部提示、小地图文字在深空背景上仍然清晰。
5. 确认小地图比例仍保持正确，且点击小地图区域不会误触地图。

### 已知问题

- 本阶段没有加入音效、震屏、粒子等更强表现；如需要，可在 M9-8 验收前追加少量表现强化。
- 当前仍是程序化 HUD 风格，后续正式美术资源替换时需要继续保留现有表现层接口。

### 下一阶段建议

人工确认 M9-7 后，进入 M9-8：最终交付整理。M9-8 应重点做完整自检、日志收口、文件清单、验收说明，并准备推送 GitHub。
## M9-8：最终交付整理

### 本阶段目标

对 M9 表现层重构进行最终收口：确认 M9-4 到 M9-7 的表现层改动可运行、玩法规则未被破坏、日志完整，并整理人工验收清单。此阶段不新增玩法，不继续扩大 UI 改动范围。

### 本阶段新增文件

- `res://presentation/ui/widgets/minimap_widget.gd`

### 本阶段主要修改文件

- `res://presentation/controllers/battle_view_controller.gd`
- `res://presentation/controllers/fx_controller.gd`
- `res://presentation/controllers/minimap_controller.gd`
- `res://presentation/layers/action_preview_layer.gd`
- `res://presentation/layers/fx_layer.gd`
- `res://presentation/layers/point_visual_layer.gd`
- `res://presentation/layers/unit_visual_layer.gd`
- `res://scripts/game_controller.gd`
- `res://scripts/hud.gd`
- `res://devlogs/M9_表现层重构_执行记录.md`

### M9-4 到 M9-7 已完成内容汇总

1. 点位与单位表现替换：
   - 玩家、NPC、资源点、技能点改为战术 HUD 风格表现。
   - 旧原型图形隐藏但保留，不破坏玩法节点。
2. 战术反馈表现：
   - 扫描范围与脉冲。
   - 攻击范围、目标线、命中/未命中反馈。
   - 技能范围、影响格与释放反馈。
   - 采集、技能点拾取、NPC 移动轨迹反馈。
3. 小地图与科技栏：
   - 新增真实小地图绘制。
   - 小地图显示安全区、黑域、玩家、可见 NPC、资源点、技能点。
   - 科技栏显示等级与可升级/锁定状态。
   - 修复小地图比例，按地图宽高比锁定，不拉伸。
4. 整体打磨：
   - 行动按钮中文文案统一。
   - 按钮和科技卡 tooltip。
   - 中心提示轻微动效。
   - HUD 文本阴影和溢出保护。

### 运行检查结果

- 主场景 headless 启动：通过。
- M4 规则测试：通过。
- M5 规则测试：通过。
- M6 AI 测试：通过。
- M8 最终自检：通过。
- 当前未发现脚本解析错误、节点路径错误或玩法自检失败。
- 已知 Godot headless 退出时仍提示 `ObjectDB instances leaked at exit` warning，该提示从此前版本延续，不影响当前测试通过结果。

### 最终人工验收清单

1. 启动主场景，确认画面风格为深空战术 HUD，背景、小地图、右侧科技栏、底部行动栏都能正常显示。
2. 玩家、NPC、资源点、技能点均不再是早期原型样式。
3. 扫描、攻击、采集、拾取技能点、释放技能、NPC 移动均有可见反馈。
4. 小地图比例正确，安全区/黑域/点位/单位位置不会明显拉伸或压扁。
5. GM 开关开启后，主地图和小地图都能显示更多 NPC 信息；关闭后回到正常可见规则。
6. 科技升级后，右侧科技卡等级同步变化。
7. 底部行动按钮、中心提示、顶部状态栏没有乱码或明显遮挡。
8. 完成一到两轮回合，确认 NPC 逐个行动、黑域推进、胜负结算仍正常。

### 已知问题与后续建议

- M9 目前以程序化战术图形为主，还不是最终商业美术资源。
- 尚未加入音效、震屏、粒子系统和完整动画状态机。
- 后续如果继续提升表现，建议进入一个新的阶段：正式美术资源替换、音效反馈、战斗节奏动画和 Web 导出专项优化。

### M9-8 结论

M9 表现层重构已完成当前文档要求的阶段性交付。建议人工验收通过后，将当前版本提交并推送 GitHub，作为 M9 完整表现层版本点。
